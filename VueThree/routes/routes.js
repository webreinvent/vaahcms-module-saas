let routes= [];

import dashboard from "./vue-routes-dashboard";
// import servers from "./routes-servers";
//
// routes = routes.concat(servers);
routes = routes.concat(dashboard);

export default routes;
